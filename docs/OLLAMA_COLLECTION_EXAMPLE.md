# Chroma MCP Ollama Collection Setup Steps

- I need to create a ChromaDB collection that uses a custom embedding model. The model [MODEL_NAME] will be executed by Ollama, and the Ollama instance is running at [OLLAMA_IP:PORT].
Please create this collection using the Chroma MCP server.

- Add an example document to this new collection.

- Show the embeddings of the added document.